worker_processes 1;
events { worker_connections 1024; }
http {
  upstream backend {
        server node:3000;
        server node2:3000;
        server reverseproxy-nginx-reverse-proxy-in-docker-bronifty.cloud.okteto.net service=https resolve;
        
    }
   server {
        listen 80;
        location / {
            proxy_pass http://backend;
        }
    }
}

